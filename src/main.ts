// YouTube Music Evolved — frontend entry point
// The main window loads music.youtube.com directly via Tauri WebviewUrl::External.
// Custom logic (title updates, etc.) is injected via initialization_script in lib.rs.
